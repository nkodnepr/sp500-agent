# Секреты — НИКОГДА не коммитить .env с реальным токеном
.env

# Виртуальное окружение
venv/
.venv/

# Python
__pycache__/
*.pyc
*.pyo

# Локальные данные бота (создаются автоматически при работе)
watchlist.db
models_cache/

# Собранный PDF инструкции — генерируется из USER_GUIDE.md,
# исходник в git есть, бинарник дублировать не нужно
USER_GUIDE.pdf

# Бэктест-графики
backtest_*.png

# Артефакты сборки .exe (PyInstaller) — генерируются заново из build_exe.py,
# коммитить бинарники в git не нужно
build/
build-venv/
dist/
dist_installer/
*.spec

# IDE
.vscode/
.idea/
*.DS_Store
