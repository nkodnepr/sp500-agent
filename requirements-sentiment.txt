# Опциональный слой зависимостей — только для sentiment-анализа новостей
# (news_sentiment.py). Без установки этого файла бот работает нормально,
# просто sentiment всегда нейтральный (0.0).
#
# torch — тяжёлая зависимость (~1-2 ГБ), первая загрузка весов FinBERT ~400 МБ.
# Устанавливать отдельно: pip install -r requirements-sentiment.txt
transformers==4.44.2
torch==2.4.1
